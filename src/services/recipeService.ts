import type { MealDBResponse, Recipe } from '../types/recipe'; const BASE_URL = 'https://www.themealdb.com/api/json/v1/1'; export const searchRecipes = async (query: string): Promise<Recipe[]> => { try { const response = await fetch(BASE_URL + '/search.php?s=' + query); if (!response.ok) { throw new Error('Failed to fetch recipes'); } const data: MealDBResponse = await response.json(); return data.meals || []; } catch (error) { console.error('Error fetching recipes:', error); throw error; } }; export const getRandomRecipes = async (): Promise<Recipe[]> => { try { const response = await fetch(BASE_URL + '/search.php?s=c'); const data: MealDBResponse = await response.json(); return data.meals || []; } catch (error) { console.error('Error fetching initial recipes:', error); return []; } };
